/* eslint-disable prettier/prettier */
import React from 'react';
import {Button, StyleSheet, Text, View} from 'react-native';

const Login = () => {
  return (
    <View>
      <Text>Hurricane Help</Text>
      <Button></Button>
    </View>
  );
};

const styles = (StyleSheet.create = {
  
})

export default Login;